package main

func main() {
	//parser := hclparse.NewParser()

	//// register a plugin from a struct instance
	//parser.RegisterPlugin(sdk.PluginInstance(myPlugin))

	//// register a plugin using a path
	//parser.RegisterPlugin(sdk.PluginPath("./blah/plugin"))
}
