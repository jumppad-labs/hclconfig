# The same container as original/web.xcl, with its two network blocks swapped

resource "container" "web" {
  network {
    id   = 2
    name = "n2"
  }

  network {
    id   = 1
    name = "n1"
  }
}
