resource "container" "first" {
  command = ["consul"]

  volume {
    source      = "."
    destination = "/cache"
  }
}

output "collection_typo" {
  value = resource.container.first.volume.0.destnation
}
