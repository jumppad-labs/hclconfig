package testfixtures

type MyType struct{}

type Options struct {
	variables      map[string]string
	variablesFiles []string
}

func process() {

	//parser := config.NewParser(options)
	//parser.RegisterType("mytype", MyType{})
	//parser.RegisterType("mytype2", MyType{})

	//// create a new Config
	//cfg := config.NewConfig()

	//// returns new config containing resources from passed in config and those in file
	//cfg = parser.ParseSingleFile(cfg)

}
